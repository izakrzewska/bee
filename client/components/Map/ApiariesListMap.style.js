import { makeStyles } from "@material-ui/core/styles";
import pallete from "../../style/pallete";

const useApiariesListMapStyles = makeStyles({
  mapContainer: {
    minHeight: "450px",
    height: "450px",
    width: "100vw",
    position: "absolute",
    left: "0"
  }
});

export default useApiariesListMapStyles;
